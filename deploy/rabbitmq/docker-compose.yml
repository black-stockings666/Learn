services:
  rabbitmq:
    image: rabbitmq:4-management
    container_name: videonest-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: videonest
      RABBITMQ_DEFAULT_PASS: videonest_rabbit_2026